{
  "name": "teni-starogo-korolevstva",
  "version": "1.0.0",
  "private": true,
  "description": "Мобильная текстовая RPG на HTML/CSS/JS с подготовкой к Capacitor",
  "scripts": {
    "sync:web": "mkdir -p www && cp -r index.html css js assets www/",
    "cap:sync": "npm run sync:web && npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  }
}
